# cookiecutter config file, as explained in :
# http://cookiecutter.readthedocs.io/en/latest/advanced/user_config.html
# values specified here will override the default ones in cookiecutter.json

default_context:
    project_name: "{{ name }}"
    wp_url: "http://{{ wp_host }}/{{ parent }}/{{ name }}"
    wp_title: "{{ site_title }}"
    wp_db_host: "db:3306"
    wp_db_user: "root"
    wp_db_password: "passw0rd!"
    wp_db_name: "{{ db_name }}"
    wp_admin_user: "admin"
    wp_admin_email: "admin@email.com"
    wp_admin_password: "passw0rd"
    wp_editor_user: "{{ username }}"
    wp_editor_password: "{{ pwd }}"
    wp_editor_email: "{{ email }}"
